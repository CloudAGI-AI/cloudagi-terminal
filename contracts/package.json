{
  "name": "cloudagi-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "CloudAGI on-chain programs — mandate-intent, receipt-mint, escrow-stake",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test --skip-local-validator",
    "test:local": "anchor test",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "lint": "eslint tests/ migrations/ --ext .ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.0",
    "@solana/web3.js": "^1.95.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "chai": "^4.3.10",
    "mocha": "^10.3.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.3"
  }
}
