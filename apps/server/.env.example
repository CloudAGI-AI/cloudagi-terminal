# Port the HTTP server listens on
PORT=3001

# Runtime environment: development | production | test
NODE_ENV=development

# PostgreSQL connection string (optional — required for persistence)
# DATABASE_URL=postgresql://user:password@localhost:5432/cloudagi

# Solana cluster endpoint: mainnet-beta | testnet | devnet
SOLANA_CLUSTER=devnet
