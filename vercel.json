{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun install --frozen-lockfile && bun run --cwd packages/shared build",
  "buildCommand": "bun run --cwd apps/web build",
  "outputDirectory": "apps/web/.next"
}
